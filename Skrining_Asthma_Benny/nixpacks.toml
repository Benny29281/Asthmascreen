[phases.setup]
nixPkgs = ["python311", "gcc"]

[phases.install]
cmds = [
  "python -m pip install --upgrade pip",
  "pip install -r backend/requirements.txt",
]

[start]
cmd = "gunicorn --chdir backend server:app --bind 0.0.0.0:$PORT --timeout 120"
